//함수형 언어에서는 변숫값 변경이 금지되어있다.

/* 
    이름이 붙은 상슈 => 변수
    한 줄에 여러개의 변수를 생성할 수 있지만, 
    가독성을 위해 1줄->1개의 변수로 작성한다.
    코드가 길어져도 가독성을 생각해야한다.
*/

let admin, Name;
/*
    let = 변수 생성 키워드
    변수는 딱 한번만 선언해야 에러가 발생하지 않는다.
    참조할 경우 let없이 변수명만 사용한다.
*/

Name = "John";
admin = "Name";

alert(admin); 

